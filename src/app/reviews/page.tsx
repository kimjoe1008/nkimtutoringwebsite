import Background from "../background";
import NavBar from "../navbar";

export default function Reviews(){
    return(
        <main>
            <Background/>
            <NavBar/>
        </main>
    );
}